// E2E tests for reports module — to be implemented in step 9
// Uses Testcontainers (real Postgres) and Supertest
describe('Reports (e2e)', () => {
  it.todo('GET /reports/summary returns totals for authenticated user');
  it.todo('GET /reports/progress/:exerciseId returns progress over time');
});
